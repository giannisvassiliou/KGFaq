The files included in this folder:

1)main.py : reads the sparql queries in Results.xlsx and its responds in response_folder (contains json style replies of the queries from endpoint)
            and via the OPENAI API, translates the queries/responses to plain English questions/answers
2)input_resutls.json: The ENDPOINT's response to the queries (created by parser)
3)Results.xlsx: The actual sparql queries (most frequent used), created by parser
